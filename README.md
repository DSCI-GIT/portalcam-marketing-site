# SplatCap 3D Website

Static marketing website for SplatCap 3D.

The site is designed for basic static hosting, including GitHub Pages or a standard web server. The public flow uses the root `index.html` page and the HTML pages inside `/pages/`.
